# Deprecated — merged into `frontend/`

The storefront PWA now lives in **`frontend/src/store/`** as part of the unified React app.

- Store: `/`
- Admin: `/admin`

Use `cd frontend && npm run dev` (single dev server on port 5173).
